export default defineAppConfig({
  /** Display name used in header, titles, and branding (text next to the logo). */
  appName: 'My Corp',
  /**
   * Logo mark only (icon). App name is rendered separately beside it.
   * Use a public path, e.g. `/logo.svg`.
   */
  logoUrl: '/logo.svg',
})
